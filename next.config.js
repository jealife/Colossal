/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports ={
    images: {
        domains: ['github.com','images.unsplash.com','pushy-mapusaurus-373.notion.site'],
    },
    nextConfig
} 
